import React from "react";
import { LiaUserEditSolid } from "react-icons/lia";
import { IconsProps } from "./Types";

const IconEdit = ({ size }: IconsProps) => {
  return <LiaUserEditSolid fontSize="inherit" size={size} />;
};

export default IconEdit;
